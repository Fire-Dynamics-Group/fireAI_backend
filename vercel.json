{
    "version": 2,
    "devCommand": "uvicorn main:app 0.0.0.0 --port 3000",
    "builds": [
        {
            "src": "*.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {"src": "/start", "dest": "/start.py"},
        {"src": "/chat", "dest": "/chat.py"},
        {"src": "/check", "dest": "/check.py"}
    ]
}
